import {Dimensions} from 'react-native';

export const {
  width: SCREEN_WIDTH,
  height: SCREEN_HEIGHT,
  fontScale: DEVICE_FONT_SCALE,
  scale: DEVICE_SCALE,
} = Dimensions.get('screen');

export const TAB_HEIGHT = 72;
